#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_cross-env@5.1.3@cross-env/dist/bin/cross-env-shell.js" "$@"
  ret=$?
else 
  node  "$basedir/../_cross-env@5.1.3@cross-env/dist/bin/cross-env-shell.js" "$@"
  ret=$?
fi
exit $ret
