#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_selenium-server@3.8.1@selenium-server/bin/selenium" "$@"
  ret=$?
else 
  node  "$basedir/../_selenium-server@3.8.1@selenium-server/bin/selenium" "$@"
  ret=$?
fi
exit $ret
