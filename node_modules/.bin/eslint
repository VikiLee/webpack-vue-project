#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_eslint@3.19.0@eslint/bin/eslint.js" "$@"
  ret=$?
else 
  node  "$basedir/../_eslint@3.19.0@eslint/bin/eslint.js" "$@"
  ret=$?
fi
exit $ret
